components/HelloButton.jsx

import React from "react";

export default function HelloButton({ onClick }) {
  return (
    <button onClick={onClick}>
      Hello Studio
    </button>
  );
}

styles/

assets/